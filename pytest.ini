[pytest]
markers =
    slow: spawns a real uvicorn subprocess (the true process-restart proof)
    property: a Hypothesis-generated test. Applied automatically at collection by the root
        conftest.py, never by hand. Deselected from the per-commit CI suite and run by the
        nightly workflow instead (Task 3.3 Boundaries).
