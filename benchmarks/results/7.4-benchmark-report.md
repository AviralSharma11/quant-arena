# Task 7.4 — the benchmark report

**Complete for the work this host can support.** B1, B2, B3, the public feed's conflation delay,
market quality under load, one optimisation with numbers on both sides, the `trace` tool and the
plots are all recorded below. What remains is not more analysis but a second machine: §9 lists
every claim that is bounded by everything having run on one laptop, and this file says so rather than leaving a reader to guess which sections are evidence.

Goal 5 asks for measured performance instead of unsupported claims, and Open Issue 012 §1 puts
the difficulty precisely: *the hard part is not making the system fast, it is measuring it in a
way that survives scrutiny.* So the methodology comes before the numbers, and §9 — what was not
tested — is not an afterthought.

---

## 1. Methodology

### Coordinated omission, and how it was avoided

The standard load generator sends a request, waits for the response, and sends the next. If the
system stalls for 200 ms the generator stalls too, never issues the requests that *would have
arrived* during the stall, and never records their latency. The stall vanishes from the
measurement and p99 looks excellent.

`benchmarks/bench_e2e.py` is open loop. Every send time is computed before the run starts, as
`t0 + i / rate`; one task per order fires at its own instant regardless of what any other order
is doing; and **latency is measured from the intended send time, not the actual one**. When the
gateway slows, nothing here slows with it — the in-flight count grows and the measurement shows
it.

That guarantee is only as good as the harness's own punctuality, so `send_slip` — intended send
time to actual send time — is measured and reported at every rate. It is the reader's check on
whether a row describes the exchange or describes Python's event loop. **Rows where slip is
large are marked and not quoted.**

### Distributions, never averages

Every figure below is p50/p95/p99/p99.9/max by nearest rank on the sorted samples, so each
number printed is a latency that actually happened to some order. No mean appears in this
document.

### Warm-up

Five seconds discarded at every rate in B2; 20,000 orders per workload in B1. Cold pages, a cold
allocator and Redis AOF behaviour all differ during that interval.

### Figures

Generated offline by `benchmarks/plot_results.py` from the JSON each harness writes into
`benchmarks/results/data/`, so any figure can be redrawn from a recorded run without touching the
exchange. In `benchmarks/results/plots/`:

| Figure | Question |
|---|---|
| `b2-latency-vs-rate.png` | does end-to-end latency hold as throughput rises, and is the harness keeping up? |
| `conflation-delay.png` | does the public feed's delay match the 20 Hz window it should? |
| `scaling-update-delay.png` | where does update delay degrade, and does the order path follow it? |
| `market-quality.png` | do spread and depth move with load? |

### Hardware, software, configuration

| | |
|---|---|
| Machine | Apple M4 · 10 cores · 16 GB · macOS 26.6.2 (Darwin 25.6.0) |
| Redis | 7.4.11, container, `appendfsync always` |
| Python | 3.13.15 (containers) |
| matplotlib | 3.11.2, for the offline plots only — never in a service |
| C++ | g++ (GCC) 13.4.0, `-std=c++20 -O2 -Wall -Wextra -Werror` — the Dockerfile's exact flags |
| `config_hash` | `7a00d4f2c68dff8336721b248b1285ddaea7bc718c52f13989054c3ac89d757e` |
| `schema_version` | 2 |
| Commit | `117f688` |

### Topology — read this before any B2 number

**Everything ran on one laptop:** gateway, matcher, ledger, fan-out, archiver, bots, Redis,
PostgreSQL, the market bots' order flow, and the benchmark harness itself, all sharing ten cores.
Task 7.4's Boundaries call for a scratch deployment; there is no scratch deployment, because
Task 3.3's deployment half is on hold pending a deployment target. This is stated here rather
than worked around, and it is the first entry in §9.

The effect is not uniform. Serialisation counts and per-hop *orderings* are exact. Tail latencies
are pessimistic — processes that would not compete for cores in a real deployment are competing
here. The knee in §3 is therefore a property of this host, not of the design.

---

## 2. B1 — the native engine

Pure C++ against the shipped order book: no Python, no Redis, no network. 200,000 orders per
workload after 20,000 discarded.

    g++ -std=c++20 -O2 -Wall -Wextra -Werror -I. -o bench engine/cpp/order_book.cpp engine/cpp/bench_order_book.cpp
    ./bench --orders 200000 --warmup 20000

"Orders per second" is meaningless without saying which orders, so there are three workloads:
`rest` never crosses and measures book growth, `cross` crosses every time and measures the
matching path, `mixed` walks a mid with roughly one order in eight priced through the spread.

Per-order cost, nanoseconds:

| workload | fills | p50 | p95 | p99 | p99.9 | max | orders/sec |
|---|---|---|---|---|---|---|---|
| rest | 0 | 292 | 750 | 1,791 | 5,750 | 675,667 | 2,637,581 |
| cross | 181,522 | 166 | 375 | 500 | 625 | 13,209 | 6,054,069 |
| mixed | 158,379 | 125 | 1,041 | 1,500 | 2,708 | 19,708 | 3,117,118 |

The harness measures its own clock too, because the quantity being timed is close to the cost of
timing it: two back-to-back `steady_clock::now()` calls cost a p50 of 42 ns locally, and inside
the container the timer is quantised to ~41.7 ns. Every per-order figure above should be read as
carrying that granularity.

**Matching costs ~166 ns at the median**, against the ~1 µs Open Issue 012 §3 budgeted. The
engine is faster than its own design estimate by a factor of six.

---

## 3. B2 — end to end, HTTP in, WebSocket out

Load is dealt round-robin across eight accounts, because `max_orders_per_second` is 1,000 per
user and a single-account ramp would measure the rate limiter. `429`s are counted, never retried
— a retry would reintroduce coordinated omission through the side door. Probe orders rest one
tick above zero so they never trade, keeping the fill path out of a measurement about the
request path.

### The knee

Coarse ramp, 20 s measured after 5 s warm-up, milliseconds:

| rate | accepted | errors | ack p50 | ack p99 | priv p50 | slip p99 | |
|---|---|---|---|---|---|---|---|
| 100 | 2,000 | 0 | 5.13 | 13.31 | 28.24 | 1.04 | |
| 200 | 4,000 | 0 | 4.71 | 190.10 | 31.35 | 4.09 | |
| 400 | 8,000 | 0 | 9.78 | 1,436.58 | 46.68 | 134.26 | soft — slip is 9% of p99 |
| 800 | 14,788 | 126 | 81,931 | 161,264 | 86,058 | 2,928 | **not evidence** |

The 800/s row is discarded. A `send_slip` p99 of 2.9 s means the harness was that late onto the
wire, so the row measures this event loop. The mechanism is worth recording because it bounds
single-process open-loop generation generally: when the server slows, tens of thousands of
pending tasks crowd the event loop's ready queue and delay the wakeups of orders that have *not
yet been sent*, so server slowness leaks backwards into the send schedule. Above roughly 300/s on
this host, load has to come from more than one process to stay honest.

Fine ramp where slip stays under 3 ms throughout, and is therefore quotable:

| rate | ack p50 | ack p99 | ack max | priv p50 | ptail p50 | slip p99 |
|---|---|---|---|---|---|---|
| 150 | 5.04 | 54.66 | 121.10 | 35.07 | 32.60 | 1.05 |
| 200 | 4.38 | 12.57 | 35.95 | 33.38 | 31.13 | 0.97 |
| 250 | 6.22 | 83.14 | 165.75 | 37.75 | 33.38 | 1.69 |
| 300 | 6.88 | 262.79 | 366.07 | 41.79 | 36.41 | 2.71 |

The median is flat at 4.4–6.9 ms while p99 climbs from 12.6 to 262.8 ms. A stable median with a
degrading tail is queueing onset: **the knee is between 200 and 300 orders/sec on this host.**

These four rows are single runs. Run-to-run variance is visible in them — 200/s scoring a better
p99 than 150/s is noise, not a finding — and the repeated runs that would let a tail be quoted
with confidence are listed in §9.

### What saturated first

Container CPU sampled once per second through the fine ramp, as a percentage of one core:

| process | median | p90 | max |
|---|---|---|---|
| **ledger** | **77.0** | **109.4** | 121.2 |
| gateway | 45.5 | 85.3 | 150.6 |
| fan-out | 14.8 | 27.3 | 38.4 |
| matcher | 12.7 | 45.5 | 73.5 |
| Redis | 9.2 | 15.5 | 107.5 |

The ledger — the derived read model, not the order path — is the process closest to saturating a
core, and it is the first thing to fix if this host's knee needs raising. It is worth being
precise about what that does and does not mean: the ledger is a *consumer*, so its lag does not
delay an acknowledgement. It competes for CPU with everything else on this one machine, which is
exactly the single-host artefact §1 warned about, and on separated hosts it would not.

---

## 4. The measured latency budget

Anchored on the stream itself rather than on an added tracing layer: the acknowledgement carries
`seq`, the inbound stream id, and every frame the exchange sends back carries the timestamp
needed to time its own path (Open Issue 012 §5 — the event stream already is the trace).

Per-hop, at 150–300 orders/sec, medians:

| Stage | Expected (012 §3) | Measured | |
|---|---|---|---|
| HTTP parse, auth, validation, risk, `XADD` | ~310 µs | **1.7–5.1 ms** | direct probe, five trials |
| Matching | ~1 µs | **166 ns** | B1, `cross` p50 |
| Inbound append → outbound append (engine hop) | ~150 µs | **1 ms** | direct probe, five trials |
| Outbound append → private frame at the client | ~50 µs | **~30 ms → 3.4 ms** | see §7 |
| Outbound append → public L2 frame at the client | up to 50 ms | **9.5 ms + 0–50 ms** | see §4.1 |

Two findings, both of which the design predicted in direction and missed in size:

1. **Matching is ~0.0005% of what a trader waits for** — 166 ns inside a ~31 ms path before §7's
   change, and inside a ~5.5 ms path after it. Open Issue 012 guessed 0.002%. The component
   optimised hardest is, by a wider margin than anyone estimated, the one that matters least.
2. **The 20 Hz conflation window dominated everything else** — and it dominated a path that the
   design had explicitly exempted from conflation. That is §7.

### 4.1 The public feed, measured per order

The conflated path needed its own instrument, and the first attempt at it was wrong. `bench_e2e`
originally timed a book frame against the `seq` it carried — but `conflation.py` stamps a
snapshot with `state.last_seq`, the newest record folded into it, so that subtraction gives the
time since the *most recent* update rather than the wait endured by any particular one. It read
5–9 ms against a 50 ms window, which is the kind of too-good number that should start an
investigation rather than end one.

`benchmarks/bench_conflation.py` anchors per order instead: it records `(arrival, seq)` for every
L2 frame, resolves each probe order's own outbound stream id in one pass afterwards, and takes
the first frame whose `seq` is at or past it. 120 probes at 2/s:

| | measured | uniform 0–50 ms predicts |
|---|---|---|
| min | 9.52 | 0.0 |
| p50 | 37.64 | 25.0 |
| p95 | 58.46 | 47.5 |
| p99 | 61.35 | 49.5 |
| max | 63.14 | 50.0 |

The shape is the design working: a **fixed floor of ~9.5 ms** — fan-out's stream read, applying
the record, encoding, and the socket write — with roughly 50 ms of uniform spread above it,
which is the conflation window. Read as `9.5 + uniform(0, 50)` the predicted median is ~34.5 ms
against 37.64 measured.

Two clocks are subtracted here — Redis's inside the container and the host's — so the tool
measures the offset between them and prints it with every run (−0.29 ms median, 1.02 ms spread
for the table above). It also flags any delay that comes out negative, because a frame cannot
arrive before the record it contains was appended, and an unexplained negative is how a reader
learns to distrust the rest of a table. An earlier run produced a −8.05 ms minimum from a
transient host/VM clock excursion; that is why the check exists.

**Checking §7's optimisation against itself.** Since private frames stopped waiting for the tick,
a connection receiving one is briefly `busy`, and a market frame offered to a busy connection is
skipped for that tick. A probe account watching the book on its own socket would therefore have
its acknowledgement push its own book frame into the next window. Measured both ways, 120 probes
each:

| observer | p50 | p95 | p99 | max |
|---|---|---|---|---|
| separate account (sends nothing) | 37.64 | 58.46 | 61.35 | 63.14 |
| the probe's own connection | 39.31 | 61.03 | 71.42 | 559.72 |

The median moves 1.7 ms, so the optimisation does not meaningfully cost a trader their book
feed. The tail does move, and the 559 ms maximum is a single sample worth naming rather than
averaging away. The default observer is a separate account, which measures conflation alone.

The gateway hop is the one the budget under-estimated, by roughly an order of magnitude: 1.7–5.1
ms measured against ~310 µs expected. It has not been decomposed further, and until it is,
nothing here says which of parse, auth, risk or `XADD` owns it.

---

## 5. B3 — connection scaling

`benchmarks/bench_scaling.py`. Open Issue 012 §4 defines *real-time update delay* as engine
output to arrival at a client, and connection scaling as raising the client count until that
delay degrades. Each frame carries `ts_ns`, stamped by the tick that built it, so

    arrival (host clock) − ts_ns (fan-out's clock)

is encode, the per-client write, and the socket. The 0–50 ms conflation wait is deliberately
**excluded** — it is a property of the 20 Hz choice, not of the client count, and leaving it in
would mask the onset being hunted for. Three L2 channels per client, 10 s measured per step, 100
acknowledgements sampled per step from a separate trading account. Milliseconds:

| clients | opened | refused | delay p50 | delay p95 | delay p99 | delay max | ack p50 | ack p99 | encodes/tick |
|---|---|---|---|---|---|---|---|---|---|
| 50 | 50 | 0 | 5.00 | 13.55 | 97.36 | 98.36 | 6.41 | 14.58 | 0.927 |
| 100 | 100 | 0 | 10.65 | 21.31 | 211.05 | 260.81 | 5.06 | 13.34 | 0.875 |
| 200 | 200 | 0 | 15.07 | 35.59 | 229.48 | 379.24 | 7.90 | 22.07 | 0.894 |
| 400 | 400 | 0 | 26.90 | 84.34 | 875.19 | 1418.13 | 15.45 | 54.97 | 0.965 |
| 800 | 800 | 0 | 34.31 | 1367.55 | 2191.51 | 2588.46 | 24.63 | 114.09 | 0.927 |

**Every connection opened. None was refused, at any step, up to 800.** That is the fix from Task
5.2 holding: the session-lookup pool that used to exhaust at 73 of 200 connections is a
`BlockingConnectionPool` now, and 800 simultaneous handshakes no longer produce a single refusal.

**Update delay degrades between 200 and 400 clients.** Through 200 the median stays at or below
15 ms and p95 below 36 ms. At 400 the median is 27 ms and p95 84 ms; at 800 the p95 is 1.37
seconds, which is a feed nobody would call real-time. Task 5.2's success criterion is 200
concurrent clients, and 200 is comfortably inside the healthy region.

**Serialisation is flat at 0.87–0.97 encodes per tick across a sixteen-fold range of client
counts.** Task 5.2 verified that property from 1 to 200 clients; it holds to 800. Encoding once
per symbol per tick and handing the same string to every subscriber is what makes the delay above
a *write* cost rather than a serialisation cost.

### The claim this does not get to make

Open Issue 006 §7b makes fan-out a separate process precisely so connection load cannot reach the
order path, and **acknowledgement latency does move here**: p50 from 6.41 ms at 50 clients to
24.63 ms at 800, p99 from 14.58 to 114.09.

That is not evidence against the design, and it is not evidence for it either. Everything shares
ten cores — including the harness, which is holding all 800 WebSockets in one Python process and
competing for the same CPU as the gateway it is timing. This measurement cannot separate
*"connection load reached the order path"* from *"everything on this laptop is fighting for the
same cores"*, and it is not honest to report it as though it could.

What can be said: at **200 clients**, the count the criterion names, ack p50 is 7.90 ms against
6.41 ms at 50 — inside the run-to-run noise this machine has shown throughout. Task 5.2's results
file closed by saying the tail deserved re-measuring at Task 7.4 on separated hosts. This is that
re-measurement, and the answer is that it still cannot be done on one host.

---

## 6. Market quality under load

`benchmarks/bench_market_quality.py`. Open Issue 012 §4 proposed this measurement as an addition
to the list `README.md` names, on the grounds that watching the spread widen and obligations
begin to breach as load rises says more than throughput does, and is a domain-specific result a
generic web-service benchmark cannot produce.

Load is resting probe orders at one tick. They never trade and never move a price, which is the
point: load that moved the market would widen the spread because *the harness* pushed it. What is
observed is the designated market makers' own quoting under a progressively busier system. Ten
symbols, top five levels, 20 s per step.

| offered load | snapshots | spread p50 | spread p95 | depth p50 (thin side) | obligation breaches | slip p99 |
|---|---|---|---|---|---|---|
| none (baseline) | 503 | 46.3 bps | 48.1 bps | 100 | 0 | — |
| 100/s | 2,252 | 46.4 bps | 48.0 bps | 100 | 0 | 1.00 ms |
| 200/s | 3,826 | 46.4 bps | 48.0 bps | 100 | 0 | 1.05 ms |
| 300/s | 3,927 | 46.4 bps | 48.0 bps | 100 | 0 | 1.16 ms |

**Market quality does not move.** Spread holds at 46.4 bps median and 48 bps at p95, depth holds
at 100 on the thinner side, and the bots logged **zero obligation breaches** at every rate. The
compliance figures are not instrumented here: `services/bots/obligations.py` already samples all
three obligations per quote tick and emits a structured `obligation_breach` line — its docstring
anticipates this exactly, *"a benchmark run greps for them"* — so the run reads the container's
logs afterwards and buckets each breach by timestamp. Structured logs plus offline analysis, as
Open Issue 012 settled, with no Prometheus and no Grafana.

The honest boundary on this result: 300 orders/sec is the most this host can offer without the
harness becoming the story (§3), so what is shown is that **market quality survives everything
this machine can throw at it** — not where it would break. Finding the breaking point needs the
multi-process generation §9 asks for.

### The benchmark was measuring itself, briefly

The first run reported a p95 spread of **19,907 bps**. The cause was this repository's own
litter: every probe order from this script and from `bench_e2e.py` rests at one tick and never
trades, so they accumulate in the book, and on a symbol the market makers happened not to be
quoting, a probe bid at one tick *was* the best bid. The harness now excludes levels at or below
the probe price and says so in its output. It is recorded here because the failure mode is the
dangerous one — a benchmark measuring its own residue produces a plausible number, not an
obviously broken one.

---

## 7. The optimisation — private frames no longer wait for the tick

**Before.** Open Issue 006 settles that private data is never dropped and is not conflated. The
implementation honoured the first half: `offer_private()` appended the frame to a deque, and the
20 Hz `flush()` handed it to the writer. So an acknowledgement or a fill waited a uniform
nought-to-fifty milliseconds — a 25 ms median — for a tick it had been exempted from. Never
dropped, but always waited. B2 found it: `ptail`, the span from durable append to the frame
landing on the client, sat at a flat ~31 ms at every rate, which is the signature of a fixed
wait rather than queueing.

**The change.** `offer_private()` hands over immediately. Market frames still wait for the tick,
which is what conflation is for — they are superseded, so batching them is free — while a private
frame is never superseded and there is nothing to gain by holding it.

**After.** Identical rates, 15 s after a 5 s warm-up, eight accounts, milliseconds:

| rate | ptail p50 before | after | priv p50 before | after | priv p99 before | after |
|---|---|---|---|---|---|---|
| 150 | 32.60 | **3.41** | 35.07 | **5.62** | 98.38 | **22.71** |
| 200 | 31.13 | **3.77** | 33.38 | **6.53** | 59.11 | **42.01** |
| 250 | 33.38 | **3.65** | 37.75 | **6.15** | 116.03 | **71.77** |
| 300 | 36.41 | **3.40** | 41.79 | **5.50** | 308.43 | **28.90** |

Roughly nine times faster at the median, and at 300/s an order of magnitude off the tail. The
tail had been growing with load because private writes queued behind ticks; it no longer does.

**A second run of the same rates, recorded because one run is not a measurement.** `ptail` p50 of
1.96 / 3.40 / 4.98 / 11.10 ms at 150 / 200 / 250 / 300 — against the first run's flat 3.41 / 3.77
/ 3.65 / 3.40. Both are far below the ~32 ms before, so the optimisation's effect is not in
question; what the second run shows is that the *post-change* figure climbs with rate where the
first suggested it was flat. At 300/s the two runs differ by more than a factor of three. Neither
is wrong; the range is the finding, and it is why §9 says no single tail here should be quoted.

**What the change cost, re-measured rather than assumed.** Task 5.2's own success criteria:

- *Criterion 5 — serialisation happens once per symbol per tick.* 5.726 encodes/tick at 1
  client, 5.851 at 50, 5.926 at 200. Flat in the client count, which is the property claimed.
- *Criterion 1 — 200 clients without acknowledgement latency degrading.* 3.80 ms median loaded
  against 3.95 ms quiet; the loaded case measured faster, as it did when 5.2 was first recorded.
- `tests/fanout` passes, 78 tests.

One trade, in policy and stated rather than buried: at 200 clients, frames delivered now spread
1178/1183/1191 where they were previously identical across clients. A market frame offered while
a private write is in flight is skipped for that tick — the documented slow-client policy, safe
because complete snapshots mean the next frame repairs it, and the same property that made
skipping affordable to begin with. It is a 1% spread and nobody is starved.

---

## 8. Tracing — `scripts/trace.py`

`README.md` Goal 6 asks for distributed tracing. Open Issue 012 §5 declines OpenTelemetry, a
collector and a backend, on the grounds that **the event stream already is the trace**: every
request carries a `client_order_id`, every record has a stream id that *is* its sequence number,
and every downstream record references one or the other. An order's path is therefore a query.

    python scripts/trace.py 1789541209066597 --user 70

      hop                           stream id         at (UTC)       +hop  detail
      gateway accepted                         06:46:49.072356          —  SubmitOrder from user 70
      inbound append          1789541209072-0  06:46:49.072000 ~-0.356 ms  durable — this id IS the sequence number
      engine output           1789541209074-0  06:46:49.074000 ~+2.000 ms  OrderAccepted order_id=2435249 2 @ 8493832 side=1
      fill                    1789541209074-1  06:46:49.074000 ~+0.000 ms  Fill 2 @ 8089363 maker=2435223 taker=2435249
      total                                                    ~ 1.644 ms

Three resolution paths, cheapest first, and the output says which one answered:

1. **The gateway's idempotency key**, which holds the inbound stream id for an hour. One `GET`
   and a one-record `XRANGE`. It also answers what the log cannot: an order refused by the
   gateway — risk, unknown symbol, bad price — never becomes durable, so *absent from the log*
   and *refused before the log* are indistinguishable without it.
2. **A bounded backwards scan** for anything older. It reports how many records it looked at,
   because "not found in the last 200,000" is a different claim from "does not exist".
3. **Forward from the inbound record** for the engine's answer, and then any `Fill` naming the
   `order_id` that answer assigned — a `Fill` carries order ids and never a `client_order_id`,
   so fills are only reachable through that join.

`client_order_id` is unique *per user*, not globally (Open Issue 008). Given one that belongs to
two users the tool prints both and refuses to choose; a confident trace of the wrong order is
worse than no trace.

**What it is not.** Hops between stream ids are millisecond-granular, because a stream id names
the millisecond an append landed in, truncated to that millisecond's start. Such a hop reads up
to 1 ms low and can print slightly negative — the `~` marks every one of them, and the tool says
so rather than clamping a number the reader could then not question. `trace` answers *what
happened to this order and in what order*; the latency budget in §4 is measured client-side at
nanosecond resolution by `bench_e2e.py` instead.

Five tests in `tests/scripts/test_trace.py` cover the path reconstruction, the fill join, the
pre-stream rejection, the cross-user ambiguity, and the bounded-scan report.

### One anomaly, found by the tool on its first real use

Tracing `client_order_id` 1789541146582505 (user 70) found the order in the inbound stream at
`1789541146596-0` — a market order, banded to a `tif=2` limit at 8,468,366 — with **no outbound
record of any kind**, while a resting order from the same account 10 ms earlier was answered
normally. A direct scan of 2,431 subsequent outbound records confirms nothing references it.

It does not reproduce: six further orders, three market and three limit, were all answered. The
matcher's `RestartCount` is 0 and it has been up since 2026-09-13, so this is not a restart
losing its place. The record is still in the stream as evidence.

This is reported, not diagnosed. `services/matcher/` is Dev A's, and Task 7.4 is not a licence to
debug it. It is recorded here because an inbound record with no engine answer is exactly the
class of thing the tracing requirement exists to surface, and because it is the difference
between a benchmark run and a benchmark run that was watched.

---

## 9. Limitations — what was not tested, and what these numbers do not support

- **No separated-host topology.** Everything shares ten cores with the harness. Task 7.4's
  Boundaries ask for a scratch deployment and there is none, because Task 3.3 is on hold pending
  a deployment target. Tails are pessimistic; the §3 knee belongs to this host.
- **Mostly single runs, and the variance is real.** Most tables here are one run. The one metric
  measured twice — see §7 — moved substantially between runs at the top of the range: `ptail`
  p50 at 300/s was 3.40 ms in one run and 11.10 ms in the next. Task 2.1 reported medians of
  three runs; these tables do not, so no tail here should be quoted as *the* tail, and the 300/s
  column least of all.
- **Nothing above ~300 orders/sec has been measured honestly.** The harness, not the exchange,
  is the limit above that, and the fix is multi-process generation rather than a bigger number.
- **Market quality under load is not measured.** Spread, depth and market-maker obligation
  compliance as throughput rises are a deliverable of this task and are not in this document.
- **Recovery time is not measured here.** A fan-out restart replaying 2,001,581 outbound records
  took about 30 seconds, observed once in passing while deploying §5's change. That is an
  anecdote, not a measurement, and it is recorded as one.
- **The gateway hop is not decomposed.** §4 shows 1.7–5.1 ms for everything up to and including
  the durable append; which part owns it is unknown.
- **The conflation figures in §4.1 are one run of 120 probes each**, at 2 orders/sec on an
  otherwise bot-driven market. They say nothing about the conflation delay under the load of §3,
  where a busier tick would have more to encode.
- **The engine's own C++ tests, and the differential test against the naive model, are not
  performance evidence** and nothing here re-verifies correctness under load.
- **These runs left litter in the live market.** Every probe order rests at one tick and never
  trades, so the books now hold thousands of them across all ten symbols. They are excluded from
  §6's measurements by price, and they are harmless to the market's own prices, but a future
  measurement that forgets they are there will misread the book exactly as §6's first run did.
  Clearing them is a `docker compose down -v`, which also destroys the stream.
- **The anomaly in §8 is one observation, not a measured rate.** Nothing here establishes how
  often an inbound record goes unanswered, and the six-order retry that failed to reproduce it
  is far too small a sample to call it rare.
